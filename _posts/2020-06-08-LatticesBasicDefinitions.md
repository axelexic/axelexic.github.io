---
layout: post
title: Lattices of Cryptography &mdash; Basic Results
date: 2020-06-08
tags: [Lattice, Lattice Based Cryptography, Post-quantum Cryptography]
---

```mathjax
\[
  \newcommand{\L}{\mathcal{L}}
  \newcommand{\B}{\mathbf{B}}
\]
```

## Introduction

A lattice $\L$ is a _discrete_ additive subgroup of
$\RR^n$. This means

1. $\L$ inherits the additive group structure of $\RR^n$,
2. $\L$ is equipped with a notion of distance (typically
  $\ell_2$ norm), and
3. there exits a _fixed non-zero radius_ $\epsilon$, such that around
   every point $\vec{x}$ in the lattice, there exists a ball

   $$S_\epsilon(\vec{x}) := \left \lbrace y \in \RR^n \; \Big|\; ||x -
   y|| < \epsilon \right \rbrace$$

   inside which no other lattice point can be found, i.e.,

   $$\exists \epsilon > 0,\; \forall \vec{x} \in \L:\quad S_\epsilon(\vec{x}) \cap \L = \{ \vec{x} \}.$$

The above definition is _basis independent_ and ideal for exploring
topological properties of lattices. However, it's not very useful for
computational purposes. For that, we will use the following equivalent
definition:

```Definition {#lattice-definition}

A lattice $\L$ of _rank_ $m$ and _dimension_ $n$ is defined to
be the set of _all possible_ **integer linear combination** of $m$ linearly independent basis
vectors $\vec{b}_1, \cdots, \vec{b}_m \in \RR^n$ (where $m \leq n$). If the basis is represented as columns of a matrix $\B := \begin{pmatrix} \vec{b}_1 & \cdots &  \vec{b}_m \end{pmatrix} \in \RR^{n\times m}$, then

$$\L(\B) = \B\cdot\ZZ^m := \left\{ z_1\vec{b}_1 + \cdots + z_m\vec{b}_m \;:\; z_i \in \ZZ  \right \} \subseteq \RR^n $$

The dimension of $\L$ is the dimension of the ambient vector space $\RR^n$. The rank of $\L$ is the rank of _free_ $\ZZ$-module generated by vectors $\lbrace  \vec{b}_1, \cdots, \vec{b}_m \rbrace$. Since $\L$ is a _free_ $\ZZ$-module, the rank is well defined and is independent of choice of basis.

A lattice whose rank is same as its dimension (i.e., $m = n$) is called a lattice of **full-rank**. Abusing terminology, we will use **$\ZZ$-span of $\B$** to mean the set $\B\cdot \ZZ^m$.
```

For the rest of the document, all lattices are assumed to be of full rank. We first state and prove that the two definitions presented so far are equivalent.

```Theorem [Discrete Subgroups of $\RR^n$ $\iff$ $\ZZ$-span of $\lbrace b_1, \cdots, b_m \rbrace$] {#thm-discrete-subgroup-defn-equivalence}

A subset $\L \subseteq \RR^n$ is a _discrete_ subgroup of $\RR^n$ if and only if there exist $m$ linearly independent vectors $\B := \lbrace \vec{b}_1, \cdots, \vec{b}_m \rbrace$ (where $0 < m \leq n$) such that $\L$ is the set of _all_ integer linear combinations of $\vec{b}_i$s.
```

```Proof  {#proof-discrete-subgroup-defn-equivalence}

We first prove that given any arbitrary matrix $\B \in \RR^{n\times m}$ of column-rank $m$, it's $\ZZ$-span is a subgroup of $\RR^n$ that also happens to be discrete.

######$\ZZ$-span of $\B$ $\Rightarrow$ Discrete Subgroup:

1. The $\ZZ$-span of $\B$ is clearly a subgroup of $\RR^n$ because for any $\vec{z}_1, \vec{z}_2 \in \ZZ^m$,
   $$\B\cdot \vec{z}_1 - \B\cdot \vec{z}_2 = \B\cdot (\vec{z}_1  - \vec{z}_2) \in \B\cdot \ZZ^m.$$
2. To show discreteness, given $\B$, we will compute a radius $\epsilon$ such that for any $\vec{x} \in \B\cdot\ZZ^m$, the ball $S_\epsilon(\vec{x})$ contains no other element of $\B\cdot\ZZ^m$ apart from $\vec{x}$.

   Let $\vec{x} = \B\cdot\vec{u}$ and $\vec{y} = \B\cdot\vec{v}$ be two _distinct_ lattice points in $\RR^n$ for some $\vec{u}, \vec{v} \in \ZZ^m.$ Let $\vec{z} = \vec{u} - \vec{v} \in \ZZ^m$. Since $\vec{x}$ and $\vec{y}$ are distinct $\vec{z} \neq \vec{0} \in \RR^m$. The distance between $\vec{x}$ and $\vec{y}$ is
   $$||\vec{x} - \vec{y}|| = ||\B\cdot(\vec{u} - \vec{v})|| = ||\B\cdot\vec{z}||.$$
   We will prove that this distance has a lower bound.

   Consider the action of the linear transformation $\tau : \vec{a} \mapsto \B\cdot \vec{a}$ restricted to the unit sphere $S^{m-1} := \lbrace \vec{a} \in \RR^m : ||\vec{a}|| = 1\rbrace.$ Let $T := \B\cdot S^{m-1} \subseteq \RR^n$ and let $\epsilon'$ be the length of the smallest vector in $T$, that is
   $$
      \epsilon' = \min_{||\vec{a} || = 1} || \B\cdot\vec{a} ||.
    $$

   This minimum is well defined because the unit sphere $S^{m-1}$ is compact, and the map $\vec{a} \mapsto ||\B\cdot \vec{a}||$ is continuous. (NOTE: $\vec{a}$ is an element of $\RR^m$ not $\ZZ^m$.) Furthermore, since columns of $\B$ are linearly independent, $\tau$ is injective and maps non-zero elements of $\RR^m$ to non-zero elements of $\RR^n$. Since $\vec{0} \not\in S^{m-1}$, $\vec{0} \not \in T$ and therefore $\epsilon' > 0.$

   Back to lattices. In order to prove that for distinct lattice points $\vec{x}$ and $\vec{y}$, $||\vec{x} - \vec{y}|| = ||\B\cdot \vec{z}|| > 0$, notice that $\vec{z} \in \ZZ^m$ is non-zero. Therefore, $\vec{z}$ can be written as a scaling of a unit vector  $\vec{u} \in S^{m-1}$ as
   $$ \vec{z} = ||\vec{z}||\vec{u}$$
   where
   $$ \vec{u} = \frac{\vec{z}}{||\vec{z}||} \in \RR^m.$$
   Therefore,
   $$
    ||\B\cdot\vec{z}|| = ||\B\cdot(||\vec{z}||\vec{u})|| = ||\vec{z}||\cdot||\B\cdot\vec{u}|| \ge ||\vec{z}||\cdot \epsilon' \ge \epsilon' > 0
   $$
   where we have used the inequality $\forall \vec{u} \in S^{m-1}: \; ||B\cdot \vec{u}|| \ge \epsilon'$ and the fact that the minimum norm of a non-zero integer vector $\vec{z}$ is $1$. Therefore, a ball of radius $\epsilon = \frac{\epsilon'}{2}$ around $\vec{x} \in \B\cdot\ZZ^m$ cannot contain any other lattice point since its smaller than the shorted distance between any two distinct lattice point.

Next we prove that given a discrete subgroup $\L$ of $\RR^n$, there exists a set of linearly independent basis vectors $\mathbf{B} := \lbrace \vec{b}_1, \cdots, \vec{b}_m  \rbrace \in \RR^{n\times m}$ (for some $m < n$) such that $\L = \lbrace \mathbf{B}\cdot \vec{z} \; \Big|\; \forall \vec{z} \in \ZZ^m  \rbrace$

###### Discrete Subgroup $\Rightarrow$ $\ZZ$-span

```

```Remark
While lattices in general are defined over reals, for computational
problems only _integral_ lattices are of cryptographic interest, i.e.,
lattices where $\mathbf{B} \in \ZZ^{n\times m} \subseteq \RR^{n\times m}$.
Furthermore, the _number of bits_ needed to represent the basis
vector $\vec{b}_i$ is assumed to be a fixed polynomial
in the dimension $n$. These seemingly artificial restrictions are
necessary for understanding how rank of a lattice
affects computational resources required to solve the problem.
```


